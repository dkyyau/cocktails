<%= simple_form_for(cocktail) do |f| %>
  <%= f.input :name %>
  <%# <%= f.input :image_url %>
  <%= f.input :image, as: :file %>
  <%= f.button :submit, "Add cocktail", class: "btn btn-outline-secondary" %>
<% end %>