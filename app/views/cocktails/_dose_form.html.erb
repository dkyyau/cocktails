<%= simple_form_for(dose) do |f| %>
  <%= f.input :description, label: 'Amount' %>
  <%= f.association :ingredient %>
  <%= f.button :submit, "Add ingredient", class: "btn btn-outline-secondary" %>
<% end %>